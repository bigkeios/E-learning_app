<li>
  <%= link_to user.name, user_path(user) %>
  <br>
  <% if admin? && !correct_user? %>
    <%= link_to(t(:delete_acc), user_path(user), method: "delete", 
                          data: { confirm: t(:cf_delete_user) }) %>
  <% end %>
</li>
